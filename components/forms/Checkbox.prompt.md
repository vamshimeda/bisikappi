Checkbox with a terracotta fill and cream box. Controlled (`checked`) or uncontrolled (`defaultChecked`). Optional `label`.

```jsx
<Checkbox label="Add jaggery" defaultChecked />
<Checkbox label="Extra hot" onChange={(e) => set(e.target.checked)} />
```
