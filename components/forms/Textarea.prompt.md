Multi-line text area matching the Input well. Use for notes, order instructions, reviews.

```jsx
<Textarea rows={4} placeholder="Any notes for the barista?" />
```

Props: `rows`, `invalid`, `disabled`.
