Pill-shaped quantity stepper for ordering flows — − [n] +. Controlled (`value`) or uncontrolled (`defaultValue`), clamped to `min`/`max`.

```jsx
<QuantityStepper defaultValue={1} min={1} max={10} onChange={setQty} />
```

Sizes: `sm | md`.
