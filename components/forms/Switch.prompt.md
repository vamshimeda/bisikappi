Toggle switch — terracotta when on, warm track when off. Controlled or uncontrolled, optional `label`.

```jsx
<Switch label="Notify me about new roasts" defaultChecked />
<Switch size="sm" onChange={(e) => setOn(e.target.checked)} />
```

Sizes: `sm | md`.
