Toast notification card with a colored left accent and tone glyph. Render inside your own fixed-position stack (top-right or bottom-center).

```jsx
<Toast tone="success" title="Added to your order" description="Filter Coffee × 2" onClose={dismiss} />
```

Tones: `default | success | warning | danger`.
