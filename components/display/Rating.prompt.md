Coffee-bean rating — the brand's replacement for star ratings. Read-only by default; pass `onChange` for an interactive picker. Supports half-beans, a numeric value, and a review count.

```jsx
<Rating value={4.5} showValue count={128} />
<Rating value={pick} onChange={setPick} size={24} />
```
