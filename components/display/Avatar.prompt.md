User avatar — image when `src` is set, otherwise initials from `name` on a warm terracotta disc (falls back to ☕).

```jsx
<Avatar name="Asha R" size="md" />
<Avatar src="/photo.jpg" alt="Barista" size="lg" />
```

Sizes: `xs | sm | md | lg | xl` or a pixel number.
