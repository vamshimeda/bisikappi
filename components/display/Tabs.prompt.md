Underlined tab bar driven by a `tabs` array. Controlled or uncontrolled.

```jsx
<Tabs
  tabs={[{id:'hot',label:'Hot'},{id:'iced',label:'Iced'},{id:'beans',label:'Beans'}]}
  defaultValue="hot"
  onChange={setTab}
/>
```
