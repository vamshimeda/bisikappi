Section divider. Plain by default; `decorative` shows the brass coffee-bean motif, or pass a `label` for an overline break. `vertical` for inline separators.

```jsx
<Divider />
<Divider decorative />
<Divider label="Our Menu" />
<Divider orientation="vertical" />
```
